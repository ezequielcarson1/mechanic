apiVersion: v1
kind: Service
metadata:
  name: admin-portal
  namespace: mechanic
spec:
  selector:
    app: mechanic-admin
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080    # Admin Portal accessible at localhost:30080
  type: NodePort
