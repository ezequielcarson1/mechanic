apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: mechanic
spec:
  selector:
    app: mechanic-server
  ports:
    - name: http
      protocol: TCP
      port: 3000         # Accessible on localhost:3000
      targetPort: 3000
  type: LoadBalancer
