apiVersion: v1
kind: ConfigMap
metadata:
  name: server-config
  namespace: mechanic
data:
  PORT: "3000"
  NODE_ENV: "production"
  DB_PATH: "/app/db/mechanic.db"
  PHOTOS_PATH: "/app/photos"
  S3_USE_LOCAL: "true"
  S3_BUCKET: "mechanic-photos"
